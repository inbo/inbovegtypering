% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_taxon_info.R
\name{link_taxon_info}
\alias{link_taxon_info}
\title{Link Species Information with Taxonomic Data}
\usage{
link_taxon_info(con_taxa, data)
}
\arguments{
\item{con_taxa}{A database connection object for the taxonomic database}

\item{data}{A data frame containing species records with columns 'OriginalName'
and 'ScientificName'}
}
\value{
A data frame containing the original records enriched with taxonomic
information, including:
- usageKey: Taxonomic identifier
- acceptedUsageKey: Identifier of accepted name (if different)
- species_number: List column with matching synoptic species numbers
}
\description{
Links species records with taxonomic information from various sources:
\enumerate{
\item Local taxonomic database
\item GBIF backbone taxonomy
\item Custom species list
The function attempts to match species names first using original names,
then scientific names, and finally using external services for unmatched species.
}
}
\details{
The matching process follows these steps:
\enumerate{
\item Matches original names against local taxonomy database
\item For unmatched names, tries matching scientific names
\item Remaining unmatched names are looked up in GBIF backbone
\item Finally checks against a custom species list
The function creates a list column 'species_number' containing all possible
matches from both usage keys and names.
}
}
\examples{
\dontrun{
library(inbovegtypering)
con <- connect_db_taxonomy()
test <- example_recordings |> select(1:12)
linked_records <- link_taxon_info(con, test)
}

}
